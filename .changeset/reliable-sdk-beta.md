---
"@agntz/sdk": minor
---

Add durable local runs and traces, persisted agent version references, lifecycle cleanup, end-to-end cancellation, recursive CLI validation, and strict packed-package coverage.
