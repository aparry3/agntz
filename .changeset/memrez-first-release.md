---
"@agntz/memrez": minor
---

Initial release of `@agntz/memrez` — a durable tagged memory layer for agntz agents. Provides SQLite- and Postgres-backed tagged memory with namespace grants, plus a `ResourceProvider` implementation that plugs into `@agntz/core`.
